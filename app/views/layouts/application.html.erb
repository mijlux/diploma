<!DOCTYPE html>
<html lang="">
<head>
  <title>Diploma</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body>
    <div class="navbar-title">
      <p><%=link_to 'Travel', root_path %></p>
    </div>
    <div class = "navbar">
      <p><%= link_to 'Все направления', url_for(controller: 'travels', action: 'index'), class: 'navbar-white' %></p>
      <p><%= link_to 'Выбрать подходящее', url_for(controller: 'travels', action: 'filter'), class: 'navbar-white' %></p>
    <div class = title-sm>
      <% if current_user.present? %>
<!--
        <%#= content_tag 'span',"@#{current_user.login}", class: 'mr-sm navbar-white' %>
-->
        <%= link_to "@#{current_user.login}",current_user, class: 'mr-sm navbar-white' %>
        <%= button_to 'Выйти', session_path, method: :delete, class: 'button button-main' %>

      <% else %>
        <%= link_to 'Вход', new_session_path, class: 'button button-main' %>
        <%= link_to 'Регистрация', new_user_path, class: 'button button-main' %>
      <% end %>
    </div>
    </div>
  <%if flash[:notice].present? %>
    <p class = "mb mt message message-notice"><%=flash[:notice] %></p>

  <%elsif flash[:alert].present? %>
    <p class = "mb mt message message-alert"><%=flash[:alert] %></p>
  <% end %>

  <%= yield %>

  <div class="mt-xxl footer-title">
    <p><%=link_to 'Travel', root_path %></p>
  </div>

  <div class = "footer">
    <p class = "text-center">&copy; Mikhail Konovalov 2024 </p>
  </div>

</body>
</html>
